Looking at your code, I can see the issue. The problem is in the exportToPDF function - there's a missing closing brace } before the Recommendations section. This is causing a JavaScript syntax error that prevents the entire script from loading properly.

Here's the fix - find this section in your code (around line 3970-3980):

```javascript
        // ========== MAINTENANCE ACTIVITIES (FIXED) ==========
        if (maintenance.length > 0) {
            doc.addPage();
            addColoredBar(doc, 0, colorPrimary[0], colorPrimary[1], colorPrimary[2]);
            
            doc.setFontSize(18);
            doc.setFont(undefined, 'bold');
            doc.setTextColor(colorPrimary[0], colorPrimary[1], colorPrimary[2]);
            doc.text('Maintenance Activities', 20, 25);
            
            let maintY = 40;
            const upcoming = maintenance.filter(m => m.status === 'scheduled').slice(0, 10);
            
            if (upcoming.length > 0) {
                doc.setFontSize(12);
                doc.setFont(undefined, 'bold');
                doc.setTextColor(0, 0, 0);
                doc.text('Upcoming Scheduled Tasks', 20, maintY);
                maintY += 10;
                
                upcoming.forEach(item => {
                    maintY = checkAndAddPage(doc, maintY, 30);
                    
                    // Activity name as bold
                    doc.setFontSize(10);
                    doc.setFont(undefined, 'bold');
                    doc.setTextColor(44, 85, 48);
                    doc.text(`• ${(item.activity || 'Unnamed').substring(0, 50)}`, 25, maintY);
                    maintY += 6;
                    
                    // Details line - using standard ASCII characters only
                    doc.setFontSize(9);
                    doc.setFont(undefined, 'normal');
                    doc.setTextColor(80, 80, 80);
                    
                    let dateStr = formatDate(item.date) || 'Date not set';
                    let typeStr = (item.type || 'other').replace(/-/g, ' ').toUpperCase();
                    let projectName = getProjectName(item.projectId);
                    
                    let detailText = `Date: ${dateStr}  |  Type: ${typeStr}  |  Project: ${projectName}`;
                    doc.text(detailText, 30, maintY);
                    maintY += 5;
                    
                    // Priority indicator - using text
                    let priorityText = '';
                    if (item.priority === 'high') priorityText = 'Priority: HIGH - Urgent attention required';
                    else if (item.priority === 'medium') priorityText = 'Priority: MEDIUM - Schedule soon';
                    else priorityText = 'Priority: LOW - Routine schedule';
                    
                    doc.setFontSize(8);
                    doc.setTextColor(100, 100, 100);
                    doc.text(priorityText, 30, maintY);
                    maintY += 5;
                    
                    // Description with proper wrapping
                    if (item.description) {
                        doc.setFontSize(8);
                        doc.setTextColor(120, 120, 120);
                        maintY = addWrappedText(doc, item.description.substring(0, 150), 30, maintY, 155, 4.5);
                        maintY += 3;
                    }
                    maintY += 8;
                });
            }
            
// Completed summary
const completed = maintenance.filter(m => m.status === 'completed');
if (completed.length > 0) {
    maintY = checkAndAddPage(doc, maintY, 30);
    doc.setFontSize(12);
    doc.setFont(undefined, 'bold');
    doc.setTextColor(0, 0, 0);
    doc.text('Completed Activities', 20, maintY);
    maintY += 8;
    
    doc.setFontSize(9);
    doc.setFont(undefined, 'normal');
    doc.setTextColor(80, 80, 80);
    doc.text(`${completed.length} maintenance task(s) completed`, 25, maintY);
    maintY += 10;
    
    completed.slice(-5).reverse().forEach(item => {
        maintY = checkAndAddPage(doc, maintY, 15);
        
        // Clean the activity name - remove any weird characters
        let activityName = item.activity || 'Unnamed';
        // Remove any non-printable characters and normalize spaces
        activityName = activityName.replace(/[^\x20-\x7E]/g, '').replace(/\s+/g, ' ').trim();
        
        doc.setFontSize(9);
        doc.setTextColor(40, 167, 69);
        doc.text(`✓ ${activityName.substring(0, 45)}`, 30, maintY);
        maintY += 5;
        
        doc.setFontSize(8);
        doc.setTextColor(100, 100, 100);
        let completedDate = formatDate(item.date);
        doc.text(`Completed: ${completedDate}`, 32, maintY);
        maintY += 8;
    });
}
        
        // ========== RECOMMENDATIONS (FIXED - SMALLER FONT) ==========
```

The problem is that the Maintenance section is missing a closing brace } before the Recommendations section.

Replace the entire Maintenance section with this corrected version:

```javascript
        // ========== MAINTENANCE ACTIVITIES (FIXED) ==========
        if (maintenance.length > 0) {
            doc.addPage();
            addColoredBar(doc, 0, colorPrimary[0], colorPrimary[1], colorPrimary[2]);
            
            doc.setFontSize(18);
            doc.setFont(undefined, 'bold');
            doc.setTextColor(colorPrimary[0], colorPrimary[1], colorPrimary[2]);
            doc.text('Maintenance Activities', 20, 25);
            
            let maintY = 40;
            const upcoming = maintenance.filter(m => m.status === 'scheduled').slice(0, 10);
            
            if (upcoming.length > 0) {
                doc.setFontSize(12);
                doc.setFont(undefined, 'bold');
                doc.setTextColor(0, 0, 0);
                doc.text('Upcoming Scheduled Tasks', 20, maintY);
                maintY += 10;
                
                upcoming.forEach(item => {
                    maintY = checkAndAddPage(doc, maintY, 30);
                    
                    doc.setFontSize(10);
                    doc.setFont(undefined, 'bold');
                    doc.setTextColor(44, 85, 48);
                    doc.text(`• ${(item.activity || 'Unnamed').substring(0, 50)}`, 25, maintY);
                    maintY += 6;
                    
                    doc.setFontSize(9);
                    doc.setFont(undefined, 'normal');
                    doc.setTextColor(80, 80, 80);
                    
                    let dateStr = formatDate(item.date) || 'Date not set';
                    let typeStr = (item.type || 'other').replace(/-/g, ' ').toUpperCase();
                    let projectName = getProjectName(item.projectId);
                    
                    let detailText = `Date: ${dateStr}  |  Type: ${typeStr}  |  Project: ${projectName}`;
                    doc.text(detailText, 30, maintY);
                    maintY += 5;
                    
                    let priorityText = '';
                    if (item.priority === 'high') priorityText = 'Priority: HIGH - Urgent attention required';
                    else if (item.priority === 'medium') priorityText = 'Priority: MEDIUM - Schedule soon';
                    else priorityText = 'Priority: LOW - Routine schedule';
                    
                    doc.setFontSize(8);
                    doc.setTextColor(100, 100, 100);
                    doc.text(priorityText, 30, maintY);
                    maintY += 5;
                    
                    if (item.description) {
                        doc.setFontSize(8);
                        doc.setTextColor(120, 120, 120);
                        maintY = addWrappedText(doc, item.description.substring(0, 150), 30, maintY, 155, 4.5);
                        maintY += 3;
                    }
                    maintY += 8;
                });
            }
            
            // Completed summary
            const completed = maintenance.filter(m => m.status === 'completed');
            if (completed.length > 0) {
                maintY = checkAndAddPage(doc, maintY, 30);
                doc.setFontSize(12);
                doc.setFont(undefined, 'bold');
                doc.setTextColor(0, 0, 0);
                doc.text('Completed Activities', 20, maintY);
                maintY += 8;
                
                doc.setFontSize(9);
                doc.setFont(undefined, 'normal');
                doc.setTextColor(80, 80, 80);
                doc.text(`${completed.length} maintenance task(s) completed`, 25, maintY);
                maintY += 10;
                
                completed.slice(-5).reverse().forEach(item => {
                    maintY = checkAndAddPage(doc, maintY, 15);
                    
                    let activityName = item.activity || 'Unnamed';
                    activityName = activityName.replace(/[^\x20-\x7E]/g, '').replace(/\s+/g, ' ').trim();
                    
                    doc.setFontSize(9);
                    doc.setTextColor(40, 167, 69);
                    doc.text(`✓ ${activityName.substring(0, 45)}`, 30, maintY);
                    maintY += 5;
                    
                    doc.setFontSize(8);
                    doc.setTextColor(100, 100, 100);
                    let completedDate = formatDate(item.date);
                    doc.text(`Completed: ${completedDate}`, 32, maintY);
                    maintY += 8;
                });
            }
        }  // <-- THIS CLOSING BRACE WAS MISSING!
```

The key fix is adding the closing brace } right before the Recommendations section. This properly closes the Maintenance section's if statement.

After making this change, your PDF export should work correctly with the completed activities showing properly as:

```
✓ Post-planting Audit
  Completed: 15/10/2025
```
