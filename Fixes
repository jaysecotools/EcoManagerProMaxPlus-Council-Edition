Absolutely! Here are the exact sections to replace:

Step 1: Replace the CSS for .main-content and add new styles

Find this section in your CSS (around line 80-100 in the style tag):

```css
/* Main Content - FIXED */
.main-content {
    margin-left: var(--sidebar-width);
    padding: 30px;
    transition: var(--transition);
    min-height: 100vh;
    width: calc(100% - var(--sidebar-width));
    overflow-x: hidden; /* Changed from auto to hidden */
    position: relative;
    z-index: 1;
}

/* Ensure container-fluid doesn't cause overflow */
.container-fluid {
    padding: 0;
    overflow-x: hidden;
}

/* Fix for row within main-content */
.main-content .row {
    margin-left: 0;
    margin-right: 0;
    width: 100%;
}

/* Ensure all cards and content stay within bounds */
.main-content .card,
.main-content .col-md-*,
.main-content .col-* {
    width: 100%;
    max-width: 100%;
}

/* Fix for the specific chart container that might be causing issues */
.main-content .col-md-8,
.main-content .col-md-4 {
    padding-left: 15px;
    padding-right: 15px;
}

/* Mobile responsiveness fix */
@media (max-width: 992px) {
    .main-content {
        margin-left: 70px;
        width: calc(100% - 70px);
        padding: 20px;
    }
}

@media (max-width: 768px) {
    .main-content {
        margin-left: 0;
        margin-bottom: 70px;
        width: 100%;
        padding: 15px;
    }
}
```

Replace it with this:

```css
/* Main Content - CRITICAL FIX */
.main-content {
    flex: 1;
    padding: 30px;
    transition: var(--transition);
    min-height: 100vh;
    min-width: 0;
    overflow-x: auto;
    position: relative;
    z-index: 1;
}

/* Ensure container-fluid doesn't cause overflow */
.container-fluid {
    padding: 0;
    overflow-x: hidden;
}

/* Fix for flex layout */
.d-flex {
    display: flex !important;
    align-items: flex-start;
    width: 100%;
}

/* Sidebar fix for flex layout */
.sidebar {
    flex-shrink: 0;
    width: var(--sidebar-width);
    position: relative;
    min-height: 100vh;
}

/* Fix for row within main-content */
.main-content .row {
    margin-left: 0;
    margin-right: 0;
    width: 100%;
}

/* Ensure all cards and content stay within bounds */
.main-content .card,
.main-content .col-md-*,
.main-content .col-* {
    width: 100%;
    max-width: 100%;
}

/* Fix for the specific chart container that might be causing issues */
.main-content .col-md-8,
.main-content .col-md-4 {
    padding-left: 15px;
    padding-right: 15px;
}

/* Mobile responsiveness fix */
@media (max-width: 992px) {
    .sidebar {
        width: 70px;
        overflow: hidden;
    }
    
    .sidebar .logo span {
        display: none;
    }
    
    .sidebar .nav-link span {
        display: none;
    }
    
    .sidebar .nav-link i {
        margin-right: 0;
        font-size: 1.4rem;
    }
    
    .sidebar:hover {
        width: var(--sidebar-width);
    }
    
    .sidebar:hover .logo span {
        display: inline;
    }
    
    .sidebar:hover .nav-link span {
        display: inline;
    }
    
    .sidebar:hover .nav-link i {
        margin-right: 12px;
    }
    
    .main-content {
        padding: 20px;
    }
}

@media (max-width: 768px) {
    .container-fluid > .d-flex {
        flex-direction: column;
    }
    
    .sidebar {
        width: 100%;
        height: 70px;
        min-height: 70px;
        bottom: 0;
        top: auto;
        position: fixed;
        z-index: 1000;
    }
    
    .sidebar .logo {
        display: none;
    }
    
    .sidebar .nav {
        flex-direction: row;
        overflow-x: auto;
        padding: 0.5rem;
    }
    
    .sidebar .nav-item {
        flex-shrink: 0;
    }
    
    .sidebar .nav-link {
        padding: 0.5rem;
        margin-bottom: 0;
        margin-right: 5px;
        border-radius: 5px;
        text-align: center;
        font-size: 0.8rem;
    }
    
    .sidebar .nav-link i {
        margin-right: 0;
        display: block;
        margin-bottom: 0.25rem;
        font-size: 1.2rem;
    }
    
    .main-content {
        margin-left: 0;
        margin-bottom: 70px;
        padding: 15px;
        width: 100%;
    }
}
```

Step 2: Replace the HTML structure

Find this exact HTML (around line 550-570):

```html
<div class="container-fluid p-0">
    <div class="row g-0">
        <!-- Sidebar -->
        <div class="sidebar">
```

Replace it with this:

```html
<div class="container-fluid p-0">
    <div class="d-flex">
        <!-- Sidebar -->
        <div class="sidebar">
```

Step 3: Find and close the d-flex div properly

Find this line (near the end of the main content, around line 1850-1870):

```html
            </div>
        </div>
    </div>
</div>
```

Replace it with this:

```html
            </div>
        </div>
    </div>
</div>
```

(Note: The closing tags are the same, just make sure you have the correct structure)

Step 4: Add this JavaScript fix at the very end of your script

Find this line near the end of your JavaScript (around line 4570):

```javascript
    // Start the app when the DOM is loaded
    document.addEventListener('DOMContentLoaded', initApp);
```

Add this code right BEFORE that line:

```javascript
    // Force layout recalculation on window load
    window.addEventListener('load', function() {
        const mainContent = document.querySelector('.main-content');
        if (mainContent) {
            mainContent.style.opacity = '0.99';
            setTimeout(function() {
                mainContent.style.opacity = '1';
            }, 10);
        }
        window.dispatchEvent(new Event('resize'));
    });
```

Summary of changes:

1. CSS: Changed the .main-content from using margin-left and width calculations to using flex: 1 for automatic width
2. CSS: Changed .sidebar from position: fixed to position: relative in desktop view
3. HTML: Changed the outer wrapper from row g-0 to d-flex for proper flexbox layout
4. Added: New mobile responsive fixes for the flex layout

These changes will make the sidebar and main content work as a proper flexbox layout, eliminating the overlap issue. The main content will now correctly sit next to the sidebar without being covered.
