I need help refactoring and cleaning up my EcoManager Pro web application code. The current code has significant issues:

**Problems I've identified:**
1. Multiple duplicate functions (e.g., ensureProperLayout appears 3+ times, fixLayout appears twice, fixTableScrolling appears multiple times)
2. Duplicate navigation handlers (sidebar navigation AND quick action handlers with similar but not identical code)
3. Redundant CSS rules that conflict with each other
4. Inconsistent function definitions scattered throughout the file
5. Mobile layout issues caused by conflicting CSS selectors

**What I need:**
Please provide a clean, refactored version of my code that:

1. **Removes all duplicate functions** - each function should be defined once
2. **Consolidates navigation logic** into a single, clean function that handles both sidebar clicks and quick action buttons
3. **Cleans up CSS** - remove conflicting/duplicate rules, organize logically (base styles → desktop → tablet → mobile)
4. **Organizes JavaScript** into logical sections with clear comments:
   - App initialization
   - Data layer (db functions)
   - UI rendering functions
   - Event handlers
   - Utility functions
   - Weather functions
   - Map functions
5. **Fixes the mobile layout issue** where Recent Activities and Upcoming Maintenance appear on all pages (they should only show on Dashboard)

**I will provide my complete HTML/CSS/JS file in the next message.**

Please analyze the code and provide a clean, working refactored version with all duplicates removed and the mobile layout issue fixed.
