I see the issue! You have a duplicated and malformed loadWeatherData() function in your code. The function appears twice with inconsistent structure. Here's the fix:

Problem:

Your loadWeatherData() function has duplicate code and is missing proper structure. Look at lines around the weather section - you have:

1. First incomplete version with duplicate updateHistoricalWeatherChart() call
2. Then the full version with the proper code

Solution: Replace the entire loadWeatherData() function with this corrected version:

```javascript
// Enhanced loadWeatherData function
async function loadWeatherData() {
    // Try to get location from projects first
    const projects = db.getProjects();
    let lat, lon;
    
    // Find first project with coordinates
    const projectWithCoords = projects.find(p => p.latitude && p.longitude);
    
    if (projectWithCoords) {
        lat = projectWithCoords.latitude;
        lon = projectWithCoords.longitude;
    } else {
        // Default to Burnie, Tasmania if no project coordinates
        lat = -41.0521;
        lon = 145.9061;
    }

    // Show loading state
    document.getElementById('weather-description').textContent = 'Loading weather data...';
    document.getElementById('lastUpdate').textContent = 'Updating...';

    try {
        const weatherData = await getWeatherByCoords(lat, lon);
        
        updateWeatherDisplay(weatherData);
        updateEnvironmentalAlerts(weatherData);
        
        // Store weather data for later use
        const updateTime = new Date().toLocaleTimeString('en-AU');
        localStorage.setItem('lastWeatherUpdate', new Date().toISOString());
        localStorage.setItem('weatherData', JSON.stringify(weatherData));
        document.getElementById('lastUpdate').textContent = `Last updated: ${updateTime}`;
        
        // Update forecast chart
        updateForecastChart(weatherData);
        
        // Update historical weather chart
        updateHistoricalWeatherChart();
        
    } catch (error) {
        console.error('Failed to load weather data:', error);
        document.getElementById('lastUpdate').textContent = 'Using example data';
        
        // Always use example data when API fails
        showExampleWeatherData();
        loadMockWeatherData();
    }
}
```

Also, make sure you remove the duplicate/partial function that appears earlier. You have this section that should be removed:

```javascript
// Remove this entire section (it appears around line 600-620):
try {
    const weatherData = await getWeatherByCoords(lat, lon);
    
    updateWeatherDisplay(weatherData);
    updateEnvironmentalAlerts(weatherData);
    
    // Store weather data for later use
    const updateTime = new Date().toLocaleTimeString('en-AU');
    localStorage.setItem('lastWeatherUpdate', new Date().toISOString());
    localStorage.setItem('weatherData', JSON.stringify(weatherData));
    document.getElementById('lastUpdate').textContent = `Last updated: ${updateTime}`;
    
    // Update forecast chart
    updateForecastChart(weatherData);
    
    // ADD THIS LINE:
    updateHistoricalWeatherChart();
    
} catch (error) {
    // ... error handling ...
}
```

Complete corrected weather section structure:

Your weather functions should appear in this order:

1. getWeatherByCoords() - fetches weather data
2. simulateWeatherAPI() - simulates API response
3. showExampleWeatherData() - shows example data
4. getWeatherIcon() - icon mapping
5. updateWeatherDisplay() - updates UI
6. updateEnvironmentalAlerts() - shows alerts
7. loadWeatherData() - main function (use the corrected version above)
8. setupWeatherLocationSelector() - location selector
9. getCurrentWeatherLocation() - gets location
10. loadMockWeatherData() - mock data for charts
11. updateForecastChart() - updates forecast chart
12. updateHistoricalWeatherChart() - updates historical chart

After making these changes, your weather section should work correctly with both the current weather display and the historical weather chart showing properly.
