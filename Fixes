I understand. Let me scan your code logically for potential issues. Since I can't see your actual updated code, I'll help you identify where the problem might be based on common patterns.

Most Likely Issue: Function Scope or Typo

Based on your error "sanitizeText is not defined", here are the most likely causes:

1. Check for a typo in the function call vs definition

Look for these exact spellings in your code:

Where you CALL the function (inside exportToPDF):

```javascript
let cleanedContent = sanitizeText(text);  // Make sure it's sanitizeText (lowercase s)
```

Where you DEFINE the function:

```javascript
function sanitizeText(text) {  // Make sure it's exactly the same - sanitizeText
```

Common typos to check:

· sanitizeText vs sanitiseText (s vs z)
· sanitizeText vs sanitizeTexts (extra s)
· sanitizeText vs SanitizeText (capital S)

2. Check if the function is inside another function

If you defined sanitizeText INSIDE another function (like inside exportToPDF), but then try to call it from somewhere else, it won't work.

WRONG - Function defined too deep:

```javascript
function exportToPDF() {
    function someInnerFunction() {
        function sanitizeText() {  // Too deep!
            // ...
        }
    }
    sanitizeText("hello"); // This will fail - not accessible here
}
```

CORRECT - Function at the right level:

```javascript
function exportToPDF() {
    function sanitizeText() {  // Directly inside exportToPDF
        // ...
    }
    sanitizeText("hello"); // This works!
}
```

3. Check for missing curly braces

If you have a syntax error like a missing } before the sanitizeText function, it might not be getting defined properly.

Quick Diagnostic Test

Add this line at the VERY BEGINNING of your exportToPDF function, right after the try {:

```javascript
function exportToPDF(projectId) {
    try {
        console.log("sanitizeText function exists?", typeof sanitizeText);
        console.log("formatDateStr exists?", typeof formatDateStr);
        console.log("getProjectNameSafe exists?", typeof getProjectNameSafe);
        
        // ... rest of your code
```

This will output to the browser console what functions are actually available. If you see "undefined" for any of them, you'll know which one is missing.

The Safest Solution

To completely eliminate the scope issue, put ALL helper functions INSIDE exportToPDF at the very top, like this:

```javascript
function exportToPDF(projectId) {
    try {
        // ALL helper functions defined first
        function sanitizeText(text) {
            if (!text || typeof text !== 'string') return '';
            return text.replace(/[^\x20-\x7E\u00A0-\u00FF]/g, '').replace(/\s+/g, ' ').trim();
        }
        
        function formatDateStr(dateString, format = 'full') {
            if (!dateString) return 'Not specified';
            try {
                const date = new Date(dateString);
                if (isNaN(date.getTime())) return dateString;
                const day = date.getDate().toString().padStart(2, '0');
                const month = (date.getMonth() + 1).toString().padStart(2, '0');
                const year = date.getFullYear();
                if (format === 'short') return `${day}/${month}`;
                return `${day}/${month}/${year}`;
            } catch (e) {
                return dateString;
            }
        }
        
        function getProjectNameSafe(projectId) {
            if (!projectId) return 'Unassigned';
            try {
                const projects = db.getProjects();
                const project = projects.find(p => p.id === projectId);
                if (project && project.name) return String(project.name).substring(0, 40);
                return 'Unknown Project';
            } catch (e) {
                return 'Unknown Project';
            }
        }
        
        // Then the rest of your PDF code...
        const { jsPDF } = window.jspdf;
        // ... continue with the PDF generation
```

Question for you:

Looking at your code structure, can you tell me:

1. Is sanitizeText defined BEFORE or AFTER the exportToPDF function? (It should be before, or inside)
2. Is there any other function between where you defined sanitizeText and where exportToPDF is?
3. When you added the helper functions, did you put them inside any other function (like inside $(document).ready() or inside another function)?

If you can describe where you placed the helper functions relative to exportToPDF, I can pinpoint the exact issue.
