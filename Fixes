<body>
    <!-- Login Screen - Formerly commented out -->
    <div id="login-screen">
        <div class="login-container">
            <div class="text-center mb-4">
                <i class="bi bi-tree-fill" style="font-size: 3rem; color: var(--primary);"></i>
                <h2 class="mt-3">EcoManager Pro</h2>
                <p class="text-muted">Council Ecosystem Management System</p>
            </div>
            
            <div class="mb-4">
                <label class="form-label">Select User Role:</label>
                <select class="form-select" id="userRole">
                    <option value="viewer">Viewer (Read Only)</option>
                    <option value="staff">Field Staff</option>
                    <option value="manager">Project Manager</option>
                    <option value="admin">System Administrator</option>
                </select>
                <small class="text-muted">For demonstration purposes only</small>
            </div>
            
            <div class="mb-4">
                <label class="form-label">Password (demo):</label>
                <input type="password" class="form-control" id="demoPassword" value="council123">
            </div>
            
            <button class="btn btn-primary w-100 mb-3" onclick="login()">
                <i class="bi bi-box-arrow-in-right"></i> Enter System
            </button>
            
            <div class="text-center">
                <small class="text-muted">Version 1.0 • Council Demo Mode</small>
            </div>
        </div>
    </div>

    <!-- Temporary fix button - remove after running once -->
    <div style="position: fixed; bottom: 10px; right: 10px; z-index: 10000;">
        <button onclick="fixCorruptedData()" style="background: red; color: white; padding: 10px; border: none; border-radius: 5px;">
            FIX CORRUPTED DATA (Click once)
        </button>
    </div>

 function fixCorruptedData() {
        try {
            let maintenance = JSON.parse(localStorage.getItem('maintenance') || '[]');
            let changed = false;
            
            for (let i = 0; i < maintenance.length; i++) {
                if (maintenance[i].activity) {
                    let original = maintenance[i].activity;
                    let fixed = original
                        .replace(/&#x27;/g, "'")
                        .replace(/&quot;/g, '"')
                        .replace(/&#x2F;/g, '/')
                        .replace(/&amp;/g, '&')
                        .replace(/&lt;/g, '<')
                        .replace(/&gt;/g, '>');
                    
                    if (fixed !== original) {
                        maintenance[i].activity = fixed;
                        changed = true;
                    }
                }
            }
            
            if (changed) {
                localStorage.setItem('maintenance', JSON.stringify(maintenance));
                alert('✓ Fixed ' + changed + ' corrupted maintenance items! The page will now refresh.');
                location.reload();
            } else {
                alert('No corrupted data found. Your data is clean!');
                // Hide the button
                document.querySelector('[onclick="fixCorruptedData()"]').style.display = 'none';
            }
        } catch(e) {
            alert('Error: ' + e.message);
        }
    }

    <div class="toast-container">
        <div id="liveToast" class="toast" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-header">
                <i class="bi bi-check-circle-fill text-success me-2"></i>
                <strong class="me-auto" id="toast-title">Notification</strong>
                <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            <div class="toast-body" id="toast-message">
                Operation completed successfully.
            </div>
        </div>
    </div>
